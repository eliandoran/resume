<!DOCTYPE html>
<html>
    <body>
        <header>
            {{#person}}
                {{firstName}} {{lastName}}
                {{_ subtitle}}
                {{_ summary}}
            {{/person}}
        </header>
    </body>
</html>
